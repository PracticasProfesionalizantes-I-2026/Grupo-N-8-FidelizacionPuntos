meta {
  name: Actualizar Cliente Admin
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/api/admin/clientes/{{clienteAdminId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenAdmin}}
}

body:json {
  {
    "nombre": "Cliente Actualizado Por Admin"
  }
}

tests {
  test("200 OK", function () {
    expect(res.status).to.equal(200);
  });
}
