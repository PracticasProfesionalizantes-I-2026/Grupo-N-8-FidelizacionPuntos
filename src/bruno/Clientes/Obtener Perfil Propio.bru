meta {
  name: Obtener Perfil Propio
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/clientes/me
  auth: bearer
}

auth:bearer {
  token: {{tokenCliente}}
}

tests {
  test("200 OK", function () {
    expect(res.status).to.equal(200);
    expect(res.body.documento).to.equal("40333444");
  });
}
