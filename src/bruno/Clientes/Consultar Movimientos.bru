meta {
  name: Consultar Movimientos
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/clientes/me/movimientos
  auth: bearer
}

auth:bearer {
  token: {{tokenCliente}}
}

tests {
  test("200 OK", function () {
    expect(res.status).to.equal(200);
  });
}
