meta {
  name: Solicitar Recuperacion
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/auth/recuperar-contrasena
  body: json
  auth: none
}

body:json {
  {
    "identificador": "cliente.demo@fidelix.local"
  }
}

tests {
  test("200 OK (siempre, sin revelar existencia - RN-27)", function () {
    expect(res.status).to.equal(200);
  });
}
